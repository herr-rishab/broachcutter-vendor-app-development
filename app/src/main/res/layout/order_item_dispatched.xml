<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/orderItemStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="@color/black"
        tools:text="3 dispatched on 20/12/2018"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:fontFamily="@font/proxima_nova_semibold"
        android:layout_marginTop="@dimen/space_20"
        android:layout_marginLeft="12dp"
        />

    <TextView
        android:id="@+id/orderItemTracking"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="@color/burnt_red"
        tools:text="Tracking number (FedEx: V123456789012345) "
        app:layout_constraintLeft_toLeftOf="@id/orderItemStatus"
        app:layout_constraintTop_toBottomOf="@id/orderItemStatus"
        android:layout_marginTop="2dp"
        android:fontFamily="@font/proxima_nova_semibold"
        />

    <ImageView
        android:id="@+id/copyIcon"
        android:layout_width="22dp"
        android:layout_height="25dp"
        android:src="@drawable/ic_copy"
        android:padding="@dimen/space_5"
        app:layout_constraintLeft_toRightOf="@id/orderItemTracking"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@id/orderItemTracking"
        app:layout_constraintBottom_toBottomOf="@id/orderItemTracking"
        android:layout_marginLeft="@dimen/space_10"
        android:layout_marginRight="@dimen/space_10"
        />
</androidx.constraintlayout.widget.ConstraintLayout>
